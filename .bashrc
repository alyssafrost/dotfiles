#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

# Created by `pipx` on 2021-04-19 17:45:41
export PATH="$PATH:/home/frost7/.local/bin"
